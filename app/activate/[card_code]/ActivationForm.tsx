'use client'
import { useState } from 'react'
import { createClient } from '@/lib/supabase/client'
export default function ActivationForm({cardCode}:{cardCode:string}){const [business,setBusiness]=useState('');const [url,setUrl]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');const s=createClient();const {error}=await s.rpc('activate_card',{p_card_code:cardCode,p_business_name:business,p_target_url:url});if(error){setError(error.message);setBusy(false);return}location.href='/dashboard'}return <main className="min-h-screen bg-slate-50 p-5"><form onSubmit={submit} className="mx-auto mt-12 max-w-md space-y-5 rounded-2xl bg-white p-6 shadow"><p className="text-sm font-semibold text-indigo-600">NFC REVIEW CARD</p><h1 className="text-3xl font-bold">Activate your card</h1><p className="text-slate-600">Connect this tap-to-review card to your business.</p><label className="block text-sm font-medium">Business name<input required value={business} onChange={e=>setBusiness(e.target.value)} className="mt-1 w-full rounded-lg border p-3"/></label><label className="block text-sm font-medium">Review destination URL<input required type="url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://g.page/..." className="mt-1 w-full rounded-lg border p-3"/></label>{error&&<p className="text-sm text-red-600">{error}. Please sign in first.</p>}<button disabled={busy} className="w-full rounded-lg bg-indigo-600 p-3 font-semibold text-white disabled:opacity-50">{busy?'Activating…':'Activate card'}</button></form></main>}
