'use client'
import { useRef, useState } from 'react'
export default function QRScanner({onScan}:{onScan:(value:string)=>void}){const [error,setError]=useState('');const video=useRef<HTMLVideoElement>(null);async function start(){try{const stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});if(video.current)video.current.srcObject=stream;setError('Camera ready. Use a QR decoder library in your deployment for frame decoding.')}catch{setError('Camera permission was denied.')}}return <div className="space-y-3"><video ref={video} autoPlay playsInline className="w-full rounded-xl bg-black"/><button onClick={start} className="rounded-lg bg-slate-900 px-4 py-2 text-white">Open camera</button>{error&&<p className="text-sm text-slate-600">{error}</p>}<button className="text-sm text-indigo-600" onClick={()=>{const value=prompt('Enter card code');if(value)onScan(value)}}>Enter code manually</button></div>}
